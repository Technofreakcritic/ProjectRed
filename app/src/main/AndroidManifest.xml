<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tfc.projectred">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/group13"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".A1_Splash_Screen">
           <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".A2_Main_Menu" />
        <activity android:name=".B2_Contact_Us" />
        <activity android:name=".B7_FAQ" />
        <activity android:name=".B1_About_Na" />
        <!--
            activity
            android:name=".Navigation"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme" /
        -->
        <activity android:name=".B3_SmartNa_plus" />
        <activity android:name=".B4_Necessary_Sodium" />
        <activity android:name=".Recipes" />
        <activity android:name=".New_FAQ" />
        <activity android:name=".FoodList" />
        <activity android:name=".FoodListNew">

        </activity>
    </application>

</manifest>